import ROW_ENUM from './row-enum'

import PERMISSION_ENUM from './permission-enum'

import MODAL_ENUM from './modal-enum'

import FORM_ENUM from './form-enum'

export default {
  row: ROW_ENUM,
  permission: PERMISSION_ENUM,
  modal: MODAL_ENUM,
  form: FORM_ENUM
}
